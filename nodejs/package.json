{
  "name": "nutrition-tracker-backend",
  "version": "1.0.0",
  "description": "Nutrition tracking system backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "generate-vapid-keys": "node -e \"const webpush = require('web-push'); console.log('Public:', webpush.generateVAPIDKeys().publicKey); console.log('Private:', webpush.generateVAPIDKeys().privateKey);\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2",
    "axios": "^1.5.0",
    "zod": "^3.22.2",
    "web-push": "^3.6.1",
    "node-cron": "^3.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}